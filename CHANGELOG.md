## 1.0.1

* Fix CMake cache invalidation and fetch flatbuffers in the build-hook.

## 1.0.0

* Initial release of the Dart FFI bindings.
* Add Windows and Linux APIs for application execution, snapshots, memory,
  registers, scheduling, process and thread views, diagnostics, ports,
  callbacks, modules, and low-level hooks.
* Add native ABI and Dart test coverage.
