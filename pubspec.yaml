name: sogen
description: Native Dart FFI bindings for the Sogen userspace emulator.
version: 1.0.1
repository: https://github.com/wdestroier/sogen_dart

environment:
  sdk: ^3.12.0

dependencies:
  code_assets: ^1.2.1
  ffi: ^2.1.4
  hooks: ^2.1.0

dev_dependencies:
  ffigen: ^20.1.1
  lints: ^6.0.0
  test: ^1.26.0
